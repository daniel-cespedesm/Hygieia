pipeline {
  agent { label "Hygieia-node" }
  stages {
    stage('Build') {
      steps {
        sh 'mvn clean install'
      }
    }
    stage('Hygieia config'){
      steps {

      }
    }
    stage('Dockerize') {
      steps {
        sh 'docker-compose build'
      }
    }
    stage('')
  }
}
